<memoryBacking>
  <access mode="shared"/>
</memoryBacking>

git clone -b refactor/professionalize-project <https://github.com/apapamarkou/yabridge-gui-controller.git>
cd yabridge-gui-controller
make run

add filesystem
Driver: virtiofs
Source path: π.χ. /home/username/vm-share
Target path: vmshare

sudo mkdir -p /mnt/yabridgeGUIControllerShare

sudo mount -t virtiofs virtiofs /mnt/yabridgeGUIControllerShare

sudo apt install libfuse2t64

sudo apt update
sudo apt install python3-pyqt6 curl
